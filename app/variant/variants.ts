enum buttonVariants {
  DEFAULT,
}

enum iconVariants {
  FILLED,
  TRANSPARENT,
}

enum containerVariants {
  DEFAULT,
  FLEXED,
  WRAPPER,
  FLEX_BW,
  FLEX_END,
  FLEX_LEFT,
  FLEX_START,
  FLEX_CENTER,
  FLEX_SELF_END,
}

export { buttonVariants, containerVariants, iconVariants };
